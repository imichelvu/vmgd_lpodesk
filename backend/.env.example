PORT=5000
NODE_ENV=development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vmgd_lpodesk
JWT_SECRET=changeme_in_production
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173

# Email notifications
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFICATION_FROM=lpodesk@vmgd.gov.vu
NOTIFICATION_NAME=LPODesk

# Optional: base URL of the app for "View request" links in emails (e.g. https://lpodesk.vmgd.gov.vu)
APP_URL=
