PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/vmgd_leave
JWT_SECRET=vanuatuisbetterin2026
JWT_EXPIRES_IN=7d
CORS_ORIGIN=http://localhost:5173

# Email notifications (PSO/Manager/Director + applicant get emails)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFICATION_FROM=leave@vmgd.gov.vu
NOTIFICATION_NAME=VMGD Leave

# Optional: base URL of the leave app for "View application" links in emails (e.g. https://leave.vmgd.gov.vu)
APP_URL=
