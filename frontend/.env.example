# Frontend config: only this .env is used (no .env.local etc.).
# VITE_API_URL: use /api for local dev (Vite proxy to backend). For production set full URL including /api.
# VITE_APP_NAME: name shown in header and on login.
VITE_API_URL=/api
VITE_APP_NAME=LPODesk
